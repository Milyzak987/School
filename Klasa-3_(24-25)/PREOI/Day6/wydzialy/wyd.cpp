#include "wydlib.h"
#include <bits/stdc++.h>
using namespace std;

vector<vector<int>> Bitek(int n, array<int, K> A, array<int, K> B) {
    return vector<vector<int>>(n, vector<int>(n, 1));
}

array<int, K> Bajtazar(array<int, 9> A) { return array<int, K>{0}; }

