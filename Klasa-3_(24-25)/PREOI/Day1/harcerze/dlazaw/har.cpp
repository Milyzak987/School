#include "harlib.h"

#include <bits/stdc++.h>

using namespace std;

vector<int> Pomaluj(int n, int m, int A, int B, vector<int> _U, vector<int> _V) {
	vector<int> kolory(m);
	return kolory;
}

void Init(int A, int B) {}

int Rusz(vector<int> y) {
	return -1;
}