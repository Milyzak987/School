#include <iostream>

using namespace std;

const int MAXN = 200000;
int num[MAXN];

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int n;
    cin >> n;
    for (int i = 0; i < n; i++) {
        cin >> num[i];
    }
}